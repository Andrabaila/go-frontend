# 📘 Coding Guidelines

## 1. Общие правила

- В начале каждого файла указывать комментарий с расположением и именем файла (// src/App.tsx).
- Все импорты внутри `src` — **абсолютные**, через `@`.
- Ограничение размера файла для компонентов, функций, хуков — **не более 50 строк**.
- Код должен быть **максимально современным** (React 19+, TypeScript strict mode, современные API).
- Между краткостью и читаемостью выбираем **читаемость**.
- Код сопровождается **комментариями по существу** (причины решений, сложные места). Банальные вещи (например `i++ // увеличиваем на 1`) не комментируются.

---

## 2. Структура проекта

- Компоненты и логика группируются по **фичам/домейнам**, а не по типам.
- Общие утилиты, хуки, константы и стили находятся в `@/shared` или `@/common`.
- Один файл — **одна ответственность**.

---

## 3. Стиль кода

- ESLint + Prettier обязательны для единообразия.
- TypeScript в режиме `strict`.
- Именование:
  - **Компоненты** → `PascalCase`
  - **Хуки** → `useCamelCase`
  - **Файлы** → `kebab-case.ts(x)`

- Запрет использования `any`, использовать точные типы или `unknown`.
- Никаких «магических значений» — только константы или `enum`.

---

## 4. Комментарии и документация

- Для функций, хуков и публичных API использовать JSDoc/TypeDoc.
- Комментарии должны объяснять **почему так сделано**, а не **что делает код**.
- Если без комментариев код «непонятен» → его нужно переписать.

---

## 5. Современные практики

- React 19 features.
- Hooks вместо классовых компонентов.
- Для асинхронных данных — React Query (TanStack).
- Для состояния — Zustand или Redux Toolkit (только при необходимости).
- Минимизировать глобальное состояние.

---

## 6. Тестирование

- Бизнес-логика покрыта unit-тестами (Jest/Vitest).
- UI-компоненты тестируются через React Testing Library + snapshot.
- Критические сценарии покрывать интеграционными тестами.

---

## 7. Оптимизация

- Код разбивать на мелкие части для переиспользования.
- Тяжёлые компоненты — ленивый импорт (`React.lazy`).
- Использовать мемоизацию (`React.memo`, `useMemo`, `useCallback`) только при необходимости.

---

## 8. Git и контроль версий

- Сообщения коммитов — по [Conventional Commits](https://www.conventionalcommits.org/):

  ```
  feat: add user profile page
  fix: correct header layout
  refactor: split map component into smaller parts
  ```

- Пулл-реквесты содержат описание _что изменено и зачем_.
- Один PR = одна логическая задача.
